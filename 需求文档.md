# 需求文档与变更记录

本文件记录 qgen 项目的需求来源与每次变更的总结。后续每次需求变更，请在文末追加一条记录。

## 项目目标

用 **Golang + eino** 实现一个智能体：读取知识库清单（规章制度 / 领导讲话 / 会议纪要 / 工作方案等文档），自动为每篇文档生成用于评测的用户提问，导出为与既有测试模板（`文档提问20条-测试模板.xlsx`）一致的 Excel。

---

## 变更记录

### v1 · 初始版本：基础问题生成
- **需求**：用 golang + eino 写一个 agent，根据知识库清单自动生成问题。
- **实现**：
  - `internal/kb`：递归扫描知识库目录（`.md/.txt/.markdown`），生成文档清单并读取正文（按长度截断），标题优先取正文首个标题。
  - `internal/gen`：用 eino `compose.Chain`（`ChatTemplate → ChatModel`，OpenAI 兼容）为每篇文档生成检索型问题；稳健解析模型 JSON 输出。
  - `internal/export`：用 excelize 导出 xlsx，列对齐测试模板（`轮次/场景/问题/上传文档/来源文档/答案位置提示`）。
  - `internal/config`：yaml + 环境变量配置；`cmd/qgen` 提供并发 worker 池、重试、进度与 `-dry-run` 等 CLI。

### v2 · 接入模型与兼容性修复
- **需求**：接入实际模型（OpenAI 兼容），跑通端到端。
- **实现 / 修复**：
  - 通过环境变量配置 `BaseURL / APIKey / Model`，兼容 `ARK_API_KEY / OPENAI_API_KEY`。
  - 修复 gpt-5.x 等 beta 模型强制 `temperature=1` 的限制：新增 `QGEN_TEMPERATURE`，值 `<=0` 时不发送 temperature 参数。
  - 排障结论沉淀：`insufficient_quota` 为账户额度问题、`invalid_api_key` 为 key/端点不匹配；本地 OpenAI 兼容服务的 BaseURL 需带 `/v1`。
  - 最终在本地端点（`http://127.0.0.1:8317/v1`，模型 `gpt-5.5`）跑通。

### v3 · 引入意图识别，按意图分桶出题
- **需求**：参考政务意图分类器（`information_interpretation / guidance / draft_from_doc / other`），让生成的问题覆盖多种意图，更贴近真实评测场景。
- **决策**：每篇文档生成 5 条，分布为 信息解读 3 + 文稿撰写 1 + 以稿写稿 1（`other` 为闲聊/无关，不纳入）。
- **实现**：
  - 配置改为 `gen.intents`（intent / scene / count 分布，可配）。
  - 重写提示词：按意图分桶输出 JSON；`draft_from_doc` 要求在问题中嵌入真实原文片段。
  - 生成器解析分桶结果并按各意图裁剪条数；`场景`列记录意图类别。
  - （该版本曾额外输出 `primary_intent / need_retrieval` 金标签列，见 v4 调整。）

### v4 · 精简输出列 + 文档化 + 上传仓库
- **需求**：删除 `primary_intent`、`need_retrieval` 两列；更新 README；新增需求文档记录每次变更；暂不生成问题；上传到 GitHub 仓库 `git@github.com:concayim/qgen.git`。
- **实现**：
  - 导出列恢复为 `轮次 / 场景 / 问题 / 上传文档 / 来源文档 / 答案位置提示`，移除两列及相关字段、映射与单测。
  - 保留 v3 的意图感知出题能力（意图类别仍体现在「场景」列）。
  - 更新 README 为意图感知版；新增本需求文档。
  - 初始化 git 并推送到远程仓库。

### v5 · Electron 桌面客户端
- **需求**：用 Electron 做一个图形客户端。
- **决策**：前端用 Electron，生成逻辑复用已测试的 Go `qgen` 二进制（不重写），二者通过新增的 `-json` 进度协议通信。
- **实现**：
  - Go 端新增 `-json` 模式，输出 `scan / start / progress / warn / done / error / exit` 等 JSON 行事件；并新增 `-guidance`、`-draft` 意图条数覆盖参数。
  - `desktop/`：Electron 应用（`main.js` 主进程 spawn 二进制并经 IPC 转发事件、`preload.js` 安全桥、`renderer/` 现代化深色 UI）。
  - 支持目录/输出选择、模型与意图分布配置、预览(dry-run)、实时进度与日志、打开/定位结果文件；设置持久化到本机 `userData`（含 API Key，不入库）。
  - 二进制与 `node_modules`、`dist` 均 `.gitignore` 排除；用户用 `npm run build:agent` 自行编译。

### v6 · Docker 部署
- **需求**：提供 Dockerfile 应付 docker 部署，并把命令写进 README，上传 GitHub。
- **实现**：
  - 多阶段 `Dockerfile`：`golang:1.25-alpine` 构建（CGO 关闭、纯静态），运行阶段 `alpine` + ca-certificates；约定知识库挂载 `/kb`、输出 `/out`，`ENTRYPOINT=qgen`。
  - `.dockerignore` 排除 `desktop/`、`.git`、xlsx、本地配置等，避免污染构建上下文。
  - `docker-compose.yml` 便捷部署，支持用环境变量/`.env` 注入 key 与目录。
  - README 增加构建/运行/compose 命令，并说明容器访问宿主机本地模型需用 `host.docker.internal`。
